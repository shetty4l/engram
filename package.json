{
  "name": "engram",
  "version": "0.1.0",
  "description": "Persistent memory MCP server for AI agents",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "engram": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "analyze": "bun run src/cli.ts",
    "setup": "bun run scripts/setup.ts",
    "opencode:config": "bun run scripts/opencode-config.ts",
    "test:core": "bun test test/db.test.ts test/cli.test.ts test/capabilities.test.ts test/http.test.ts test/decay.test.ts",
    "test:full": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src/",
    "format": "biome check --write src/ test/",
    "format:check": "biome check src/ test/",
    "validate": "bun run typecheck && bun run lint && bun run format:check && bun run test:core",
    "validate:full": "bun run typecheck && bun run lint && bun run format:check && bun run test:full",
    "version:bump": "bun run scripts/version-bump.ts",
    "prepare": "husky"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "@types/bun": "latest",
    "husky": "^9.0.0",
    "oxlint": "^0.12.0",
    "typescript": "^5.0.0"
  }
}
